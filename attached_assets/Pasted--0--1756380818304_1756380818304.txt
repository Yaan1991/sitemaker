 Переделать текущий сайт (без сноса проекта)

0) Резюме задачи (что не так и что нужно)
	•	Первый экран сейчас не соответствует задумке.
	•	Нужно: Hero = автослайдер из проектов (3 карточки на desktop / 1 на mobile), автосмена каждые 2 секунды, внутри каждой карточки — картинка, заголовок, короткое описание, 2 ссылки: «Подробнее» (внутрь проекта) и «В СМИ» (наружу, если есть).
	•	Блок «Обо мне» остаётся как есть по смыслу, но тексты и отступы — как ниже.

⸻

1) Конкретные правки по файлам

Если проект на React/Vite: правим src/pages/index.jsx|tsx или App.jsx|tsx (главная), src/pages/about.jsx (страница «Обо мне»), стили — src/styles.css или модульные.
Если чистый HTML: правим index.html и styles.css.

1.1 Главная (первый экран → автослайдер)
	•	Заменить текущий первый экран на секцию section#hero-slider с слайдером на 8 карточек.
	•	Автосмена: 2000ms (2s), infinite, pauseOnHover (desktop), swipe (mobile).
	•	Одновременно видно:
	•	≥1024px → 3 карточки
	•	768–1023px → 2 карточки
	•	<768px → 1 карточка
	•	Навигация: стрелки (слева/справа) + 8 точек (индикаторы).
	•	Фон секции: bg_dark.webp (cover, center).
	•	Кнопка под слайдером, по центру: «Смотреть все проекты» → /works.

Структура карточки (каждая):
	•	Изображение 1200×800 с overlay 20–25%.
	•	Текстовый блок (внизу):
	•	title — Название + площадка/год
	•	subtitle — краткий тезис (1–2 строки)
	•	Кнопки:
	•	Подробнее → url (внутренняя страница проекта)
	•	В СМИ → press (внешняя ссылка; если press нет — кнопку скрыть)

1.2 Блок «Обо мне» на главной (после слайдера)
	•	Оставить текущую секцию «Обо мне», но заменить текст на утверждённый (см. ниже).
	•	Фото about.jpg справа (desktop) / сверху (mobile).
	•	Под текстом две ссылки: «Полное обо мне» → /about, «Список всех работ» → /works.

1.3 Хедер
	•	Фиксированный. Слева текстовый логотип «Yan Kuzmichev» → /.
	•	Справа меню: «Обо мне» → /#about, «Проекты» → /#projects (якорь на слайдер), «Все работы» → /works, «Контакты» → /#contacts.
	•	Кнопка в хедере: «Слушать» → https://band.link/zDZyK.
	•	При скролле фон: rgba(0,0,0,.75) + backdrop-filter: blur(8px).

1.4 Контакты (низ главной)
	•	Иконки PNG + ссылки:
	•	Telegram — https://t.me/iankzmcv — icon_telegram.png
	•	Email — mailto:kuzmichevyan@gmail.com — icon_email.png
	•	Телефон — tel:+79197643745 — icon_phone.png
	•	Bandlink — https://band.link/zDZyK — icon_bandlink.png
	•	Якорь секции: id="contacts".